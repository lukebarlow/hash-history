{
  "name": "hash-history",
  "description": "Store state in the hash string of the url",
  "version": "0.1.0",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "https://github.com/lukebarlow/hash-history.git"
  },
  "files": [
    "dist",
    "lib",
    "src"
  ],
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "build": "babel src --out-dir lib",
    "prepublish": "npm run build && npm run test"
  },
  "dependencies": {
    "properties-and-events": "^1.2.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.11.6",
    "@babel/core": "^7.11.6",
    "@babel/preset-env": "^7.11.5",
    "babel-jest": "^26.5.2",
    "jest": "^26.5.2",
    "mock-browser": "^0.92.12"
  }
}
